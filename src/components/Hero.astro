<style>
.hero {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 120px 10%;
  background: var(--bg-primary);
  color: var(--text-primary);
  gap: 60px;
  flex-wrap: wrap;
}

/* texto */
.hero-text {
  max-width: 520px;
}

.hero-text h1 {
  font-family: 'Space Grotesk', sans-serif;
  font-size: 3rem;
  margin-bottom: 20px;
  line-height: 1.2;
}

.hero-text p {
  color: var(--text-secondary);
  margin-bottom: 30px;
}

/* badge */
.badge {
  font-size: 0.85rem;
  color: var(--accent);
  margin-bottom: 15px;
}

/* botones */
.hero-buttons {
  display: flex;
  gap: 15px;
}

.btn-primary {
  background: var(--accent);
  color: white;
  padding: 14px 26px;
  border-radius: 8px;
  text-decoration: none;
  font-weight: 500;
}

.btn-secondary {
  border: 1px solid var(--border);
  padding: 14px 26px;
  border-radius: 8px;
  color: var(--text-primary);
  text-decoration: none;
}

/* imagen */
.hero-image img {
  max-width: 520px;
  border-radius: 12px;
}
</style>

<section class="hero">

  <div class="hero-text">
    <p class="badge">NexByte</p>

    <h1>
      Soporte Técnico & Desarrollo Web<br>
      en un solo lugar
    </h1>

    <p>
      Soluciono problemas en tus equipos y creo páginas web que generan clientes para tu negocio.
    </p>

    <div class="hero-buttons">
      <a href="#" class="btn-primary">Contactar</a>
      <a href="#" class="btn-secondary">Ver servicios</a>
    </div>
  </div>

  <div class="hero-image">
    <img src="/src/images/mockup.jpg" alt="Preview NexByte" />
  </div>

</section>
<section class="hero container">
  <div class="hero-left">
    <p class="badge">🚀 LANDING PAGES QUE CONVIERTEN</p>

    <h1>
      Sitios estáticos,<br />
      <span>resultados reales.</span>
    </h1>

    <p class="subtitle">
      Creo sitios web rápidos, seguros y optimizados para SEO
      que convierten visitantes en clientes.
    </p>

    <div class="actions">
      <a class="btn">Hablemos →</a>
      <a class="link">Ver trabajos <span class="arrow">→</span></a>
    </div>

    <div class="checks">
      <span>✔ Rápidos</span>
      <span>✔ Optimizados</span>
      <span>✔ Sin complicaciones</span>
    </div>
  </div>

  <div class="hero-right">
    <div class="orb orb-purple"></div>
    <div class="orb orb-cyan"></div>

    <div class="waves"></div>

    <div class="mockup">
      <img src="../public/mockup.png" alt="preview" />
    </div>
  </div>
</section>

<style>

.hero {
  display: grid;
  grid-template-columns: 1.1fr 1fr;
  align-items: center;
  gap: 60px;
  padding-top: 120px;
}

/* ================= TEXT ================= */

.hero-left h1 {
  font-size: 60px;
  font-weight: 800;
  margin: 20px 0;
}

.hero-left h1 span {
  background: linear-gradient(90deg, #2563EB, #22d3ee);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.subtitle {
  max-width: 520px;
}

/* ================= RIGHT SIDE ================= */

.hero-right {
  position: relative;
  height: 500px;
}

/* 🔥 ORB GLOW */

.orb {
  position: absolute;
  border-radius: 50%;
  filter: blur(100px);
  opacity: 0.6;
}

.orb-purple {
  width: 300px;
  height: 300px;
  background: #7c3aed;
  top: 50px;
  left: 50px;
}

.orb-cyan {
  width: 300px;
  height: 300px;
  background: #22d3ee;
  bottom: 0;
  right: 0;
}

/* 🌊 WAVES (esto es lo más importante) */

.waves {
  position: absolute;
  width: 600px;
  height: 400px;
  right: -50px;
  top: 50px;

  background: radial-gradient(circle at center,
      rgba(124,58,237,0.4),
      transparent 60%),
    radial-gradient(circle at center,
      rgba(34,211,238,0.4),
      transparent 60%);

  filter: blur(60px);
  opacity: 0.7;

  animation: float 6s ease-in-out infinite alternate;
}

/* 💎 MOCKUP */

.mockup {
  position: relative;
  z-index: 2;

  border-radius: 20px;
  overflow: hidden;

  box-shadow:
    0 0 40px rgba(124,58,237,0.3),
    0 0 80px rgba(34,211,238,0.2);

  transform: rotate(-6deg);
}

.mockup img {
  width: 100%;
  display: block;
}

/* ================= ANIMATIONS ================= */

@keyframes float {
  from {
    transform: translateY(0px) translateX(0px);
  }
  to {
    transform: translateY(-20px) translateX(-20px);
  }
}

/* entrada suave */

.hero-left {
  animation: fadeUp 1s ease;
}

.hero-right {
  animation: fadeUp 1.2s ease;
}

@keyframes fadeUp {
  from {
    opacity: 0;
    transform: translateY(40px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.hero-left {
  display: flex;
  flex-direction: column;
  gap: 18px; /* 🔥 clave */
}

/* badge */
.badge {
  font-size: 13px;
  letter-spacing: 1px;
  color: var(--cyan);
}

/* título */
.hero-left h1 {
  font-size: 60px;
  font-weight: 800;
  line-height: 1.1; /* 🔥 clave */
  margin: 10px 0;
}

/* descripción */
.subtitle {
  max-width: 520px;
  font-size: 16px;
  line-height: 1.6; /* 🔥 clave */
  color: var(--text-secondary);
}

.link {
  display: inline-flex;
  align-items: center;
  gap: 8px;

  font-weight: 500;
  color: var(--text-secondary);

  transition: all 0.3s ease;
  cursor: pointer;
}

.arrow {
  transition: transform 0.3s ease;
}

.link:hover .arrow {
  transform: translateX(5px);
}

.link:hover {
  color: var(--text-primary);
  transform: translateX(5px);
}

.actions {
  margin-top: 25px;
  display: flex;
  align-items: center;
  gap: 25px;
}

.checks {
  margin-top: 15px;
  display: flex;
  gap: 20px;
  font-size: 14px;
  color: var(--text-secondary);
}

</style>
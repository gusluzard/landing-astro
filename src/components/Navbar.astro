<style>
.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 10%;
  background: #0f172a;
  color: white;
}

.logo {
  font-family: 'Inter', sans-serif;
  line-height: 1;
  display: flex;
  flex-direction: column;
  align-items:center;
  justify-content: center;
}

/* línea superior */
.top {
  font-size: 2rem;
  font-weight: 800;
  text-align: center;
}

/* GL destacado */
.top {
  background: linear-gradient(90deg, #7c3aed, #22d3ee);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

/* STUDIO abajo */
.bottom {
  font-size: 1.2rem;
  letter-spacing: 7px;
  color: #94a3b8;
  margin-top: 5px;
  text-align: center;
}

/* Web Studio = secundario */
.text {
  font-size: 1rem;
  font-weight: 500;
  color: #94a3b8;
}

@keyframes glow {
  from {
    text-shadow: 
      0 0 10px rgba(124, 58, 237, 0.6),
      0 0 20px rgba(34, 211, 238, 0.4);
  }
  to {
    text-shadow: 
      0 0 20px rgba(124, 58, 237, 1),
      0 0 40px rgba(34, 211, 238, 0.8);
  }
}

.nav-btn {
  background: #22c55e;
  padding: 10px 20px;
  border-radius: 6px;
  text-decoration: none;
  color: black;
}
</style>

<nav class="navbar">
  <div class="logo">
    <div class="top">GLWEB</div>
    <div class="bottom">STUDIO</div>
  </div>
  <a href="#" class="btn">Contactar</a>
</nav>
<section class="benefits">

  <!-- IMAGEN -->
  <div class="benefits-bg">
    <img src="/benefits.png" alt="beneficios" />
  </div>

  <!-- CONTENIDO -->
  <div class="benefits-overlay">
    <div class="benefits-content">

      <div class="benefits-header">
        <h2>Beneficios de tener un sitio web</h2>
        <p>
          Descubre cómo una página web puede ayudarte a crecer,
          posicionarte y generar más oportunidades.
        </p>
      </div>

      <div class="benefit">
        <div class="check"></div>
        <div class="benefit-content">
          <h3>Más clientes</h3>
          <p>Tu sitio trabaja 24/7 captando nuevos clientes.</p>
        </div>
      </div>

      <div class="benefit">
        <div class="check"></div>
        <div class="benefit-content">
          <h3>Mayor credibilidad</h3>
          <p>Genera confianza inmediata en tus clientes.</p>
        </div>
      </div>

      <div class="benefit">
        <div class="check"></div>
        <div class="benefit-content">
          <h3>Velocidad y rendimiento</h3>
          <p>Experiencia rápida y mejor posicionamiento.</p>
        </div>
      </div>

      <div class="benefit">
        <div class="check"></div>
          <div class="benefit-content">
          <h3>Optimización SEO</h3>
          <p>Estructura optimizada para posicionarte en Google y atraer tráfico orgánico.</p>
          </div>
      </div>

      <div class="benefit">
        <div class="check"></div>
        <div class="benefit-content">
          <h3>Diseño responsive</h3>
           <p>Tu sitio se verá perfecto en celulares, tablets y computadoras.</p>
        </div>
      </div>

      <div class="benefit">
        <div class="check"></div>
        <div class="benefit-content">
          <h3>Bajo mantenimiento</h3>
          <p>Al ser sitios estáticos, no requieren mantenimiento constante ni plugins.</p>
        </div>
      </div>

    </div>
  </div>

</section>

<style>
/* SECTION BASE */

.benefits {
  position: relative;
  height: 200vh; /* 🔥 scroll real */
  margin: 0; /* 🔥 elimina espacios */
  padding-top: 0;
  padding-bottom: 0;
}

/* 🖼️ IMAGEN FULL */

.benefits-bg {
  position: sticky;
  top: 0;
  height: 100vh;
  width: 100%;
  overflow: hidden;
}

.benefits-bg img {
  width: 100%;
  height: 100%;
  object-fit: cover; /* 🔥 clave para centrar */
}

/* 🌑 OVERLAY OSCURO */

.benefits-bg::after {
  content: "";
  position: absolute;
  inset: 0;

  background: linear-gradient(
    to right,
    rgba(2,6,23,0.3),
    rgba(2,6,23,0.85)
  );
}

/* 📝 CAPA DEL TEXTO */

.benefits-overlay {
  position: absolute;
  inset: 0;
  z-index: 2;

  display: flex;
  justify-content: flex-end; /* 🔥 lado derecho */
  align-items: center;
}

/* CONTENIDO */

.benefits-content {
  width: 100%;
  max-width: 600px;

  margin-right: 80px;

  display: flex;
  flex-direction: column;
  gap: 80px;
}

/* HEADER */

.benefits-header h2 {
  font-size: 48px;
  line-height: 1.1;
}

.benefits-header p {
  color: var(--text-secondary);
}

/* ITEMS */

.benefit {
  display: flex;
  gap: 15px;
}

.benefit-content {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

/* CHECK */

.check {
  min-width: 28px;
  height: 28px;
  border-radius: 50%;

  background: linear-gradient(135deg, #7c3aed, #22d3ee);

  box-shadow:
    0 0 10px rgba(124,58,237,0.8),
    0 0 20px rgba(34,211,238,0.6);

  position: relative;
}

.check::after {
  content: "✓";
  position: absolute;
  color: white;
  font-size: 14px;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
}

</style>